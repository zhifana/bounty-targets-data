name: Sync Fork

on:
  push:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: TG908/fork-sync@v1.6.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          owner: arkadiyt
          head: main
          base: main

      - name: Get latest changes
        id: git_diff
        run: |
          CHANGES=$(git clone https://github.com/${{ github.repository }}.git && cd $(basename $_ .git) && git --no-pager diff --name-only HEAD^ HEAD)
          echo "::set-output name=changes::$CHANGES"

      - name: Send Discord Notification
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          CHANGES=$(echo "${{ steps.git_diff.outputs.changes }}")
          curl -X POST -H "Content-Type: application/json" \
            -d '{"content":"Fork sync completed!\n\nChanges:\n\n'"$CHANGES"'"}' \
            $DISCORD_WEBHOOK_URL
